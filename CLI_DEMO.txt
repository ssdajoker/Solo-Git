================================================================================
                    SOLO GIT CLI - DEMONSTRATION
================================================================================

Command: evogitctl hello
Output:
--------
🏁 Solo Git is ready!

Solo Git - where tests are the review and trunk is king.

Next steps:
  1. Configure API credentials: evogitctl config setup
  2. Initialize a repository:   evogitctl repo init --zip app.zip
  3. Start pairing with AI:     evogitctl pair 'add feature'

Run 'evogitctl --help' for more information.

================================================================================

Command: evogitctl version
Output:
--------
Solo Git (evogitctl) version 0.1.0
Python 3.11.6 (main, Sep 16 2025, 12:40:29) [GCC 12.2.0]
Abacus.ai API: ✗ not configured

================================================================================

Command: evogitctl --help
Output:
--------
Usage: evogitctl [OPTIONS] COMMAND [ARGS]...

  Solo Git - Frictionless Git workflow for AI-augmented solo developers.

  A paradigm shift in version control that eliminates branches, PRs, and
  manual reviews, replacing them with ephemeral workpads and test-driven auto-
  merging.

  Quick Start:
    evogitctl config setup              # Configure API credentials
    evogitctl repo init --zip app.zip   # Initialize from zip
    evogitctl pair "add login feature"  # Start AI pairing

  Philosophy:
    • Tests are the review
    • Single trunk, no PRs
    • Ephemeral workpads instead of branches
    • Green tests = instant merge

Options:
  --version          Show the version and exit.
  -v, --verbose      Enable verbose output
  -c, --config PATH  Path to config file (default: ~/.sologit/config.yaml)
  --help             Show this message and exit.

Commands:
  config   Configuration management commands.
  hello    Test command to verify Solo Git is working.
  pad      Workpad operations.
  pair     Start AI pairing session (coming in Phase 2).
  repo     Repository management commands.
  test     Test execution commands.
  version  Show version information.

================================================================================

Command: evogitctl config --help
Output:
--------
Usage: evogitctl config [OPTIONS] COMMAND [ARGS]...

  Configuration management commands.

Options:
  --help  Show this message and exit.

Commands:
  env-template  Generate .env template file.
  init          Initialize a new configuration file with defaults.
  path          Show configuration file path.
  setup         Setup Solo Git configuration.
  show          Display current configuration.
  test          Test API connection and validate configuration.

================================================================================

Command: evogitctl config init
Output:
--------
✅ Created configuration file at /home/ubuntu/.sologit/config.yaml

📝 Edit the file to add your API credentials:
   /home/ubuntu/.sologit/config.yaml

Or run: evogitctl config setup

================================================================================

Command: evogitctl config show
Output:
--------
📋 Solo Git Configuration

🔐 Abacus.ai API:
  Endpoint:  https://api.abacus.ai/v1
  API Key:   YOUR_API...HERE (use --secrets to show)

🤖 Models:
  Planning:  gpt-4o
  Coding:    deepseek-coder-33b
  Fast:      llama-3.1-8b-instruct

💰 Budget:
  Daily Cap:       $10.00
  Alert at:        80%
  Track by model:  True

⚙️  Workflow:
  Auto-merge on green:  True
  Auto-rollback:        True
  Workpad TTL:          7 days

📁 Paths:
  Config file:    /home/ubuntu/.sologit/config.yaml
  Repositories:   ~/.sologit/repos

================================================================================

                    ALL COMMANDS WORKING ✅

================================================================================
